export type ConsultationRequestAcceptanceParam = {
  /* eslint-disable camelcase */
  consultation_req_id: number,
  picked_candidate: number,
  user_checked: boolean
  /* eslint-enable camelcase */
}
